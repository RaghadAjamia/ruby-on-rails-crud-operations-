<div class="container">
  <div class="row justify-content-center">
    <h4 class="mt-4 mb-4 display-4">Edit Warehouse</h4>
  </div>
  <div class="row justify-content-center">
    <%= form_with model: @warehouse, local: true do |form| %>
      <div class="form-group">
        <%= form.label :name, "Warehouse Name", class: 'form-label' %>
        <%= form.text_field :name, class: 'form-control', placeholder: 'Enter warehouse name' %>
      </div>
      
      <div class="form-group">
        <%= form.label :location_id, "Location", class: 'form-label' %>
        <%= form.collection_select :location_id, Location.all, :id, :address, prompt: 'Select a location', class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.submit 'Update Warehouse', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
